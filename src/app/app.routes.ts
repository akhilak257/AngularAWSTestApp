import { Routes } from '@angular/router';
//import { UserForm } from './user-form/user-form';
import { UserFormRx } from './user-form-rx/user-form-rx';

export const routes: Routes = [
  { path: '', component: UserFormRx }
];
